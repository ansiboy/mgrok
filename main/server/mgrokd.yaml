http_addr: :8081
domain: t.mgrok.cn
# tunnel_addr                    # 隧道地址，默认为 :4443
# data_addr: 127.0.0.1:6313      # HTTP 代理用于内部数据传输的地址，默认为空  
# http_pulbish_port: 5626        # HTTP 对外访问的端口，需要反向代理时才设置。默认为空
# log_to: stdout                 # 日志输出，stdout 或 none。默认为 stdout
# log_level: debug               # 日志输出级别，可以为：DEBUG, INFO, WARNING, ERROR，默认为 DEBUG 